version: '2'
services:
  homeassistant:
    container_name: home-assistant
    image: homeassistant/home-assistant:stable
    volumes:
      - /home/pi/home-media-server/home-assistant/config:/config
    environment:
      - TZ=Europe/Berlin
    restart: always
    network_mode: host
